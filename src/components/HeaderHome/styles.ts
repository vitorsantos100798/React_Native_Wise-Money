import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  headerContainer: {
    flex: 1,
    backgroundColor: "#00ACB7",
  },

  headerImage: {
    marginLeft: 160,
    marginTop: 60,
  },
});
